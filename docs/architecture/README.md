# Architecture decisions

Store ADRs here as `adr-NNN-short-title.md` using [docs/templates/adr-template.md](../templates/adr-template.md).

Start an ADR when a decision is **hard to reverse** or **affects multiple modules** (navigation container, dependency graph, persistence technology, networking stack, feature flags).
