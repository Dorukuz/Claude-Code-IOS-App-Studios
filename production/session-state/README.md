# Session state

Create **`active.md`** while working on a long task. Hooks preview this file at session start and before context compaction.

Suggested sections:

- Current objective
- Open decisions
- Files in progress
- Commands to re-run (tests, schemes)

`session-stop.sh` archives then deletes `active.md` when a session ends—copy anything you need to sprint docs first.
