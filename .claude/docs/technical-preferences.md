# Technical preferences — Claude-Code-IOS-App-Studios

Edit this file to match **your** project. Hooks and `/start` use it to detect whether a project is still a “fresh template.”

- **Engine / platform**: iOS (native)
- **Minimum iOS**: [TO BE CONFIGURED] (e.g. 17.0)
- **Xcode**: [TO BE CONFIGURED] (e.g. 16.x)
- **Swift**: [TO BE CONFIGURED] (e.g. 6.0)
- **UI**: SwiftUI-first (UIKit where needed)
- **Package layout**: [TO BE CONFIGURED] — Xcode app target vs SwiftPM-only vs mixed mono-repo
- **Dependency managers**: [TO BE CONFIGURED] — SPM / CocoaPods / Carthage / none

When **Minimum iOS**, **Xcode**, **Swift**, and **Package layout** are all filled (no `[TO BE CONFIGURED]`), the template treats tooling as **configured**.
